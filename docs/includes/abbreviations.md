

[userdb]: https://example.com "The data you see within Mathesar lives in the `user database`, which must use PostgreSQL. Each Mathesar installation can connect to multiple user databases, potentially on different servers."
